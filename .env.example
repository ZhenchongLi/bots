# OpenAI API 统一代理服务 - 环境配置示例
# 复制此文件为 .env 并根据你的需求修改配置

# ============================================================================
# 服务配置
# ============================================================================

# 服务监听地址 (生产环境建议使用 0.0.0.0，开发环境可使用 127.0.0.1)
HOST=0.0.0.0

# 服务监听端口
PORT=8000

# 日志级别 (debug, info, warning, error)
LOG_LEVEL=info

# ============================================================================
# 数据库配置
# ============================================================================

# SQLite 数据库 URL
DATABASE_URL=sqlite+aiosqlite:///./data/proxy.db

# ============================================================================
# 日志配置
# ============================================================================

# 日志文件路径
LOG_FILE_PATH=./logs/proxy.log

# 日志保留天数
LOG_RETENTION_DAYS=30

# ============================================================================
# AI 模型配置
# ============================================================================

# 平台类型 (openai, anthropic, google, azure_openai, cohere, coze, custom)
TYPE=openai

# API 密钥 (请替换为你的实际 API 密钥)
API_KEY=your_api_key_here

# API 基础 URL
BASE_URL=https://api.openai.com/v1

# 实际模型名称 (服务会将所有请求的模型名称替换为此值)
ACTUAL_NAME=gpt-3.5-turbo

# 是否启用此模型 (true/false)
ENABLED=true

# ============================================================================
# 模型参数配置 (可选)
# ============================================================================

# 显示名称 (可选，用于标识模型)
# DISPLAY_NAME=GPT-3.5 Turbo

# 模型描述 (可选)
# DESCRIPTION=OpenAI GPT-3.5 Turbo model for general conversations

# 最大令牌数
MAX_TOKENS=4096

# 是否支持流式输出 (true/false)
SUPPORTS_STREAMING=true

# 是否支持函数调用 (true/false)
SUPPORTS_FUNCTION_CALLING=true

# 每 1K 输入令牌成本 (可选，用于成本计算)
# COST_PER_1K_INPUT_TOKENS=0.001

# 每 1K 输出令牌成本 (可选，用于成本计算)
# COST_PER_1K_OUTPUT_TOKENS=0.002

# 超时时间（秒）
TIMEOUT=300

# ============================================================================
# 平台特定配置示例
# ============================================================================

# OpenAI 配置示例
# TYPE=openai
# API_KEY=sk-your-openai-api-key
# BASE_URL=https://api.openai.com/v1
# ACTUAL_NAME=gpt-4

# Claude (Anthropic) 配置示例
# TYPE=anthropic
# API_KEY=sk-ant-your-claude-key
# BASE_URL=https://api.anthropic.com/v1
# ACTUAL_NAME=claude-3-sonnet-20240229

# Gemini (Google) 配置示例
# TYPE=google
# API_KEY=your-google-api-key
# BASE_URL=https://generativelanguage.googleapis.com/v1
# ACTUAL_NAME=gemini-pro

# Azure OpenAI 配置示例
# TYPE=azure_openai
# API_KEY=your-azure-api-key
# BASE_URL=https://your-resource.openai.azure.com/openai/deployments/your-deployment
# ACTUAL_NAME=gpt-4

# Coze Bot 配置示例
# TYPE=coze
# API_KEY=your-coze-api-key
# BASE_URL=https://api.coze.com/v1
# ACTUAL_NAME=coze-bot
# BOT_ID=your-bot-id
# CONVERSATION_ID=optional-conversation-id

# 本地模型 (Ollama) 配置示例
# TYPE=custom
# API_KEY=not-needed
# BASE_URL=http://localhost:11434/v1
# ACTUAL_NAME=llama2

# ============================================================================
# 开发配置 (生产环境请根据需要调整)
# ============================================================================

# 开发模式下的额外配置
# CORS_ORIGINS=["http://localhost:3000", "http://localhost:8080"]
# DEBUG=false

# ============================================================================
# 注意事项
# ============================================================================
# 1. 请确保 API_KEY 的安全性，不要提交到版本控制系统
# 2. 生产环境建议使用更强的数据库配置
# 3. 根据你的使用量调整 LOG_RETENTION_DAYS
# 4. 修改配置后需要重启服务才能生效
# 5. 所有以 # 开头的行都是注释，可以安全删除